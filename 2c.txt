// Name: Ivy Loi
// Assignment: HW 1
// Problem 2c
// 3/2/26

//Pseudocode for 2c
#include <iostream>
identicalElements(std::list<int>elementList) {
    for every element in elementList:
        int current = 0;
        int next = 0;
        if (current == next):
            move to next element in list;
        else:
            print out "Not every element in the sequence is identical";
            return 1;
    return elementList;
}