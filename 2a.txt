// Name: Ivy Loi
// Assignment: HW 1
// Problem: 2a
// Date: 3/2/26
//Pseudocode for 2a
#include  <iostream>
#include <cmath>

int squareRootCompute(int number) {
    int root = 0;
    //Checking if square root is imaginary
    if (number < 0) then:
        print out "The number is negative, cannot compute square root because root is imaginary";
        return 1;
    else
        root^2 == number;
        print out "The root of (number) is (root)";
    
    return root;
}
